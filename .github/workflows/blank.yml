name: make-puzzle-CI

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      - name: compile check
        run: docker run --rm -v $(pwd):/home/opam/satysfi amutake/satysfi satysfi ${pwd}/test/test.saty -o ${pwd}/test/test.pdf
